// reducers/index.js
import { } from '../actions';

const initialState = {
	screen1: "about",
	screen2: "contact"
}

export default (state = initialState, action) => {
  switch (action.type) {
    
    default:
      return state;
  }
};
