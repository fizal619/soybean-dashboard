// reducers/index.js
import { } from '../actions';

const initialScreens = {
	screen1: "about",
	screen2: "contact"
}
export default (state = initialScreens, action) => {
  switch (action.type) {
    
    default:
      return state;
  }
};
